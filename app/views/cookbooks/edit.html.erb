<% @title="WC? | Edit Cookbook" %>

<div class="white-bg col-md-5">
	<h1>Edit: <%= @cookbook.title %></h1>
	<% if current_user == @cookbook.user %>
		<%= render 'form' %>
	<% else %>
		You are not permitted to edit this cookbook.
	<% end %>
	<br><br>
</div>

